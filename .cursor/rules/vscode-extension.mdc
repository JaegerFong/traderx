---
description: VS Code 扩展开发约定（命令、侧栏、状态持久化）
globs: "src/**/*.{ts,tsx}"
alwaysApply: false
---

# VS Code Extension 约定

## 命令与 UI

- 命令命名与展示文案保持一致、可搜索（例如 `TraderX: ...`）。
- 所有用户可见错误信息要可行动：给出下一步（重试/检查网络/打开设置/查看日志）。

## 状态与隐私

- 成本价、持仓股数等属于用户敏感信息：只保存在本机状态，不写入任何远程；日志中默认脱敏/不打印。
- 分组、排序等 UI 偏好应“可恢复”：每分组独立记忆并在下次启动沿用（遵循现有实现）。

## Webview / Simple Browser

- 打开分时等外链页面要考虑失败兜底：优先内置打开，失败时回退系统浏览器，并保留原始 URL 便于排查。

## 网络与数据源（扩展特性）

- 外部行情/资金接口无 SLA：解析要防御性（字段缺失、类型变化、编码差异），并避免让单个股票/接口失败拖垮整体刷新。
- 刷新频率与资源消耗：保持最小间隔约束（例如 3 秒）一致；避免在 UI 线程做重 CPU 工作。
