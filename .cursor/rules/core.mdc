---
description: 核心协作规范（适用于本仓库所有工作）
alwaysApply: true
---

# 核心规范

- 默认使用中文沟通；技术名词、代码符号与路径使用英文并用反引号标注（如 `src/http.ts`）。
- 修改行为优先：先读现有实现与约定，再改动；避免“另起炉灶式”重写。
- 变更要可回滚：改动尽量小步、聚焦、可解释；避免把无关格式化混到功能改动里。
- 错误处理要明确：不要吞错；需要降级时要说明降级策略与影响面。
- 日志与隐私：不要在日志/错误信息里泄露 token、cookie、用户自定义持仓/成本等敏感信息。
- 网络/外部依赖：对外部接口保持防御性（超时、重试、字段缺失/变更兼容）。
- 写代码前先对齐目标：如果需求含糊，先给出 2-3 个可选方案与推荐项，再实施。
