---
description: TypeScript/JavaScript 代码质量与工程约定
globs: "**/*.{ts,tsx,js}"
alwaysApply: false
---

# TS/JS 质量约定

## 类型与可维护性

- 优先使用显式类型与窄化；避免无意义的 `any`（确需使用时在局部收敛并解释原因）。
- 避免隐式 `as` 断言“糊过去”；更偏好运行时校验（尤其是外部 HTTP 响应）。
- 需要导出给其他模块使用的 API，优先定义清晰的类型/接口并保持向后兼容。

## 错误处理

- 不要空 `catch`；要么记录并重新抛出，要么转换为更高层可理解的错误。
- 异步链路中优先保留根因（例如包装错误时保留 `cause` 或至少保留原始 message）。

## Promise / 并发

- 只有在确认彼此独立时才并行（`Promise.all`）；有依赖关系就保持顺序执行。
- 清晰设置超时与取消：涉及 `fetch` 的调用应有超时参数，并能被上层控制。

## 依赖与构建

- 新增依赖要有理由：说明为什么需要它、替代方案是什么、对 bundle/启动的影响。
- 修改 `package.json` 脚本时保持向后兼容，避免破坏现有开发/调试流程（`npm run watch`、`npm run compile`）。
